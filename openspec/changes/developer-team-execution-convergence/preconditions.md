# Apply Preconditions

None.

Rationale: no external condition blocks starting Apply. Unproven runner hooks and incomplete rollout observation windows are explicit observe/shadow activation gates handled by conformance tasks, not prerequisites for implementing the compatible runtime slices.
