# Preconditions: Keep AGENTS.md as a Repository Guide

- User explicitly requested an architecture-only root guide and removal of runtime memory/Developer Team policy from that file.
- Current source confirms Codex roles and skills already receive capability instructions independently of the root block.
- Current root `AGENTS.md` has one legacy marker block and `.codex/deck-manifest.json` records its current whole-file hash.
- Existing user work in `.serena/project.yml` is out of scope and must be preserved.
- The local Skill Registry is invalid; bounded direct source discovery is used without refreshing or modifying it.
- No network, real runner installation, provider call, or user-home write is authorized for this change.
