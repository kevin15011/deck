# Preconditions — OpenCode Package Install Running Binary Regression

## Status

None.

The excluded active-session forced upgrade path depends on a future upstream release, but it is not a precondition for this install-missing repair. Automated checks must use injected or disposable paths and must not mutate live user configuration or processes.
