# Preconditions: Support macOS SSH Alias Project Identity

- The user explicitly requires Deck to manage valid SSH aliases without Git remote workarounds.
- The current stable binary reproduces `managed-runtime-project-missing` for `git@work:comodin-software/espritec-theme.git` on macOS.
- Existing canonical project identity, project-isolation, and fail-closed memory requirements remain authoritative except for the deliberate addition of a supported Darwin account-home strategy.
- `.serena/project.yml` is pre-existing user work and is outside this change.
- No release, publication, Git remote mutation, SSH configuration mutation, commit, or push is authorized.
- Skill-registry validation reported malformed frontmatter; automated Setup repair could not acquire its modifying authorization provider, so work continues through official OpenSpec/source context and configured runner capabilities.
